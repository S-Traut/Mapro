<div class="articles-pop" style="margin-top: 4em">
	<div class="titre">
		<h3>Articles populaire</h3>
	</div>

	<div class="row row-cols-3 row-cols-md-6 g-4">
		{% for article in articles %}
			<div class="col">
				<a href="/article/{{ article.id }}" class="card h-100">
					<img src="{{article.getImage.0.image}}" class="img-thumbnail" alt="..." style="height: 140px;">
					<div class="card-body">
						<h5 class="card-title">{{article.nom}}</h5>
						<p>{{article.prix}}
							€</p>
					</div>
				</a>
			</div>
		{% endfor %}
	</div>

</div>
